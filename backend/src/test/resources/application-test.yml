spring:
  datasource:
    url: jdbc:tc:postgresql:14-alpine:///test_database
    username: user
    password: password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  flyway:
    enabled: false