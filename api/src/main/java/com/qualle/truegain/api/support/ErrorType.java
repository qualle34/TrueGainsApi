package com.qualle.truegain.api.support;

public enum ErrorType {

    SERVER_ERROR,
    BAD_REQUEST,
    VALIDATION_FAIL,
    CONFIRMATION_FAIL,
    NOT_FOUND,
    ACCESS_DENIED,
    EXPIRED_TOKEN,
    UNAUTHORIZED
}
